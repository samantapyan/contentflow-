# Copy to .env and adjust as needed.
NODE_ENV=development
PORT=3000

# Must match the credentials in docker-compose.yml
DATABASE_URL=postgresql://contentflow:contentflow@localhost:5432/contentflow?schema=public
